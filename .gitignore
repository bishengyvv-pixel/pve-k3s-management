# ----------------------------------------------------
# 1. 敏感配置和本地环境文件 (SECURITY)
# ----------------------------------------------------
# 绝对不要提交包含敏感信息（如 API 密钥、密码）的 .env 文件。
# 部署时，用户应该复制 .env.example 并手动创建 .env 文件。
*.env
.env.*.local
.env.local

# ----------------------------------------------------
# 2. Python 运行时和虚拟环境 (DEPENDENCIES & ARTIFACTS)
# ----------------------------------------------------
# 忽略所有虚拟环境目录
venv/
env/

# 忽略所有依赖项构建的目录 (对应您的 mcp/ 结构)
# mcp/lib/
# mcp/bin/
# mcp/include/
mcp/bin/
mcp/lib/
mcp/include/
mcp/lib64/

# 忽略 Python 编译和临时文件
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*~
.ropeproject

# 忽略构建和打包文件
dist/
build/
*.egg-info/
.coverage

# ----------------------------------------------------
# 3. 特定工具生成的锁文件/构建文件
# ----------------------------------------------------
# 忽略 uv/pip-compile 锁文件 (如果您希望严格控制依赖，可取消注释)
# uv.lock 

# 忽略 PyInstaller 临时文件 (对应 main_mcp.spec)
*.spec

# ----------------------------------------------------
# 4. IDE 和操作系统文件
# ----------------------------------------------------
.DS_Store             # macOS
.vscode/              # Visual Studio Code 配置
.idea/                # PyCharm/IntelliJ IDEA 配置
thumbs.db             # Windows 缩略图缓存

# ----------------------------------------------------
# 5. NFS/Cloud-init 临时/输出文件
# ----------------------------------------------------
# 如果您的脚本会在这些目录下创建日志或其他临时输出文件，请在此处添加
# 例如，如果您在 k3s_dir/images 下创建了临时的 .img 文件
nfs/images/*
