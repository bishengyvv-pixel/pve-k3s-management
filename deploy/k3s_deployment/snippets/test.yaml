#cloud-config
users:
  - name: bisheng
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    shell: /bin/bash
chpasswd:
  list: |
    bisheng:password
  expire: false
ssh_pwauth: true

package_update: true
package_upgrade: true
packages:
  - qemu-guest-agent
  
runcmd:
  - systemctl enable --now qemu-guest-agent
